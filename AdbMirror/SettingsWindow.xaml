<Window x:Class="AdbMirror.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="220"
        Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource LabBackgroundBrush}"
        Foreground="{DynamicResource LabTextPrimaryBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,12">
            <TextBlock Text="General"
                       Style="{StaticResource LabHeadingTextBlock}"
                       FontSize="14"
                       Margin="0,0,0,8" />

            <CheckBox Content="Auto mirror when device connects"
                      IsChecked="{Binding AutoMirrorOnConnect}"
                      Margin="0,0,0,6" />

            <CheckBox Content="Start scrcpy in fullscreen"
                      IsChecked="{Binding StartFullscreen}"
                      Margin="0,0,0,12" />

            <TextBlock Text="Default quality preset"
                       Style="{StaticResource LabBodyTextBlock}"
                       Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding Presets}"
                      SelectedItem="{Binding DefaultPreset}"
                      Width="160"
                      Style="{StaticResource LabComboBox}" />
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="80"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Style="{StaticResource LabGhostButton}" />
            <Button Content="OK"
                    Width="80"
                    IsDefault="True"
                    Click="OnOkClicked"
                    Style="{StaticResource LabPrimaryButton}" />
        </StackPanel>
    </Grid>
</Window>


